package com.example.controller;

import com.example.model.Organization;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;

@Controller
public class OrganizationController {

    // Show the organization details form
    @GetMapping("/organization")
    public String organizationForm(Model model) {
        model.addAttribute("organization", new Organization());
        return "organizationForm"; // This maps to organizationForm.html
    }

    // Handle form submission
    @PostMapping("/organization")
    public String submitOrganization(Organization organization) {
    	
    	
        // Save the organization details to the database or perform other actions
        // For now, let's redirect to a success page
    	
    	
        return "organizationSuccess"; // Redirect to a success page or another view
    }
}
