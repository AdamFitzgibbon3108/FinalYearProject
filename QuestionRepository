package com.example.repository;

import com.example.model.Question;
import org.springframework.data.jpa.repository.JpaRepository;
import java.util.List;

public interface QuestionRepository extends JpaRepository<Question, Long> {

    // Example method to find questions by difficulty
    List<Question> findByDifficulty(String difficulty);

    // Example method to find questions by control category
    List<Question> findByControlCategory(String controlCategory);

    // You can add more custom queries based on your needs
}
